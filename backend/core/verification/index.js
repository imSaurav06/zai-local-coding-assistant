"use strict";

const { runVerification, verificationErrors, verificationResult } = require("./verificationEngine");

module.exports = {
    runVerification,
    verificationErrors,
    verificationResult
};
