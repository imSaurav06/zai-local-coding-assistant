"use strict";

const shadowRuntimeErrorCodes = Object.freeze({
    SHADOW_RUNTIME_INVALID_INPUT: "SHADOW_RUNTIME_INVALID_INPUT",
    SHADOW_RUNTIME_FAILED: "SHADOW_RUNTIME_FAILED"
});

module.exports = {
    shadowRuntimeErrorCodes
};
