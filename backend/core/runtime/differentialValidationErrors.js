"use strict";

/**
 * Frozen error codes for differential validation.
 */
const differentialValidationErrorCodes = Object.freeze({
    DIFFERENTIAL_VALIDATION_FAILED: "DIFFERENTIAL_VALIDATION_FAILED",
    DIFFERENTIAL_REPORT_INVALID: "DIFFERENTIAL_REPORT_INVALID"
});

module.exports = {
    differentialValidationErrorCodes
};
