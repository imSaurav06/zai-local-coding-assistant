"use strict";

const runtimeMetricsErrorCodes = Object.freeze({
    RUNTIME_METRICS_INVALID: "RUNTIME_METRICS_INVALID",
    RUNTIME_METRICS_CORRUPTED: "RUNTIME_METRICS_CORRUPTED"
});

module.exports = {
    runtimeMetricsErrorCodes
};
