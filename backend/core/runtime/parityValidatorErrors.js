"use strict";

const parityValidatorErrorCodes = Object.freeze({
    PARITY_VALIDATION_FAILED: "PARITY_VALIDATION_FAILED",
    PARITY_INVALID_RESULT: "PARITY_INVALID_RESULT"
});

module.exports = {
    parityValidatorErrorCodes
};
