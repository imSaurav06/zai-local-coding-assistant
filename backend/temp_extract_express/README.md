# BooksAPI

![Backend](https://img.shields.io/badge/Backend-Express.js-68a063?style=flat-square)  ![Database](https://img.shields.io/badge/Database-MongoDB-47a248?style=flat-square)

A **Express REST API** generated by Z.ai.

## ✨ Features

- Books API — CRUD operations for books resource
- Health Check — API health status endpoint

## 🛠 Tech Stack

- **Backend:** Express.js (Node)
- **Database:** MongoDB with Mongoose

## 📋 Prerequisites

- Node.js v18+
- MongoDB (local or Atlas)

## 🚀 Getting Started

```bash
# 1. Clone or extract the project

# 2. Install dependencies
npm install

# 3. Start the server
npm run dev   # or: npm start
```

## 🔐 Environment Variables

Copy `.env.example` to `.env` and fill in the values:

```bash
cp .env.example .env
```

| Variable | Description |
|----------|-------------|
| `PORT` | Required — see `.env.example` for details |
| `MONGODB_URI` | Required — see `.env.example` for details |

## 📡 API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/api/books` | Get all books |
| `GET` | `/api/books/:id` | Get a single book by ID |
| `POST` | `/api/books` | Create a new book |
| `PUT` | `/api/books/:id` | Update a book by ID |
| `DELETE` | `/api/books/:id` | Delete a book by ID |
| `GET` | `/health` | Check API health status |

## 🗄 Data Models

### Book
| Field | Type |
|-------|------|
| `title` | String |
| `author` | String |
| `publishedYear` | Number |
| `genre` | String |

## 📁 Project Structure

```
.env.example
README.md
package.json
server.js
models/
  Book.js
```

## 📦 Downloaded ZIP Usage

```bash
# Extract the downloaded ZIP file, then:
npm install
npm run dev      # development
npm run build    # production build
```

## 📝 License

This project was scaffolded by [Z.ai](https://github.com/imSaurav06/zai-local-coding-assistant). MIT License.
